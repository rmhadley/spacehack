name: tcod-freeze

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify the tcod reference freeze
        run: python3 tools/tcod_freeze.py
